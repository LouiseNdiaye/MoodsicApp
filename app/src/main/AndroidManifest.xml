<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.android.activity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.android.activity.TestActivity"
            tools:ignore="Instantiatable"></activity>
        <activity android:name="com.android.activity.CalendarActivity"
            tools:ignore="Instantiatable" />
        <activity android:name="com.android.activity.ResultActivity"
            tools:ignore="Instantiatable" />
        <activity android:name="com.android.activity.QuestionActivity"
            tools:ignore="Instantiatable" />
        <activity android:name="com.android.activity.MenuActivity"
            tools:ignore="Instantiatable" />
        <activity
            android:name="com.android.activity.MainActivity"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>